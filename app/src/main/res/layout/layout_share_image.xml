<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/background"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/iv_imageShare_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="fitCenter"
        android:background="@color/background"
        android:adjustViewBounds="true"
        tools:src="@color/white"/>

    <!--<ImageView
        android:id="@+id/iv_imageShare_watermark"
        android:layout_width="@dimen/ic_size_small"
        android:background="@color/transparent"
        app:srcCompat="@drawable/im_default"
        android:layout_alignBottom="@id/iv_imageShare_content"
        android:layout_alignRight="@id/iv_imageShare_content"
        android:layout_marginRight="@dimen/margin_small"
        android:layout_marginBottom="@dimen/margin_small"
        android:visibility="gone"
        android:layout_height="@dimen/ic_size_small" />-->

</RelativeLayout>